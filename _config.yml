defaults:
  -
    scope:
      path: ""
    values:
      layout: default
    scope:
      path: "submodule-test"
    values:
      layout: collaboration
    scope:
      path: "open-data-portal-requirements"
    values:
      layout: collaboration
